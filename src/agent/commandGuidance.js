export const COMMAND_EXECUTION_GUIDANCE = `Command selection rules:
- Decide before every shell call whether the command is a bounded foreground operation or a managed background job.
- Use execute_command only when there is strong reason to expect completion within 30 seconds. Good examples: pwd, ls, git status, reading a small file range, and one small targeted test.
- For anything that may take 30 seconds or more, has unknown duration, or starts training, a server, watcher, lengthy build/test, download, migration, or batch job: call wait_command with the command. That single call starts a managed job and blocks until it finishes or emits logs — there is no separate start step. When uncertain, choose wait_command. If neither managed command tool is available, do not force long work through execute_command; explain that this runtime cannot manage the long job.
- Use start_command only when you must not wait now: the job should keep running while you do other work in this turn, or a schedule_wakeup continuation will pick up its result later.
- Never use execute_command to "try" a long command first. A timeout is a failure boundary, not a scheduling strategy.
- Pass managed command tools the normal foreground form of the command. Never add nohup, &, disown, screen, tmux, or a shell timeout wrapper; managed command tools own detachment, logs, status, and process-tree cancellation.
- Command tools have no interactive stdin. Use non-interactive flags and explicit input; do not launch a command that will wait for a prompt.
- Keep the returned job_id and nextCursor. Use get_command for an immediate status check. Use wait_command with job_id to keep waiting: pass wait_seconds covering the expected remaining time (default 30, up to 7 days) so one wait suffices instead of a tight loop of short re-waits. The user sees the wait duration while it runs.
- Use schedule_wakeup to end the turn now and resume later — when the session should be freed for other work, the wait must survive restarts or disconnects, or no interactive result is needed. Schedule a future continuation when schedule_wakeup is available, with a self-contained prompt containing the job_id and latest nextCursor. Otherwise report the job_id so it can be checked later.
- Sparse or silent output does not prove a job is stuck. Do not stop a running job merely because it has produced no recent logs. Use stop_command only when the user requested cancellation, the job has clearly failed, or it is no longer useful.`;
